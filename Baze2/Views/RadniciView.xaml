<Window x:Class="Baze2.RadniciView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Baze2"
        mc:Ignorable="d"
        Title="RadniciView" Height="305" Width="515">
    <Grid>
        
        <DataGrid Grid.Column="1" x:Name="dataGrid" SelectionChanged="DataGrid_SelectionChanged" AutoGenerateColumns="False" Height="265" HorizontalAlignment="Left" VerticalAlignment="Top" Width="500" SelectionMode="Single" IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Width="*" Binding="{Binding Id}" />
                <DataGridTextColumn Header="Ime" Width="*" Binding="{Binding radIme}" />
                <DataGridTextColumn Header="Prezime" Width="*" Binding="{Binding radPrz}" />
                <DataGridTextColumn Header="lokalID" Width="*" Binding="{Binding LokalId}" />
            </DataGrid.Columns>
        </DataGrid>

    </Grid>
</Window>
